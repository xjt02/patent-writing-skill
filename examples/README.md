# 测试样本说明

> 本目录样本基于 `${SKILL_ROOT}\Desktop\专利\` 下公开样例脱敏生成，**仅用于 skill 开发与测试**，不构成任何法律建议。

## 脱敏原则

1. **去掉作者姓名**（示例发明人 → "[发明人]"）
2. **去掉具体应用场景**（"高铁站区泵房" → "工业现场"），保留通用性
3. **保留核心算法逻辑**与权利要求结构
4. **保留文字描述中暗含的公式占位**（用 `<<<FORMULA:...>>>` 标记）

## 样本清单

| 文件 | 类型 | 用途 |
|---|---|---|
| `sample_001_pointer_meter.md` | 图像识别 / 无公式 | 测试 5 节结构、权利要求编号、流程图 IR |
| `sample_002_lightweight_digit.md` | 图像识别 / 含公式 | 测试 OMML 公式嵌入、MathML → OMML 转换 |

## 来源对照

| 样本 | 原 .docx | 字段 |
|---|---|---|
| sample_001 | `(发明已通过)一种基于HRNet的指针式仪表识别方法-示例发明人-v2.docx` | HRNet / 指针式仪表 / 仪表数值 |
| sample_002 | `(发明已通过)一种基于轻量化网络的数字识别方法-示例发明人-241126.docx` | 轻量化网络 / 数码管 / 数字识别 |

## 公式占位标记

样本 2 用 `<<<FORMULA:mathml_content>>>` 标记应转换为 OMML 的位置，例如：

```
损失函数：<<<FORMULA:L=-\frac{1}{N}\sum_{i=1}^{N}y_i\log(\hat{y}_i)>>>
```

skill 的 `prompts/05_formula_omml.md` + `tools/mathml_to_omml.py` 负责解析这些标记并生成 OMML。